# built binary
/gatt

# extracted graphs + vector indexes (contain private schema metadata)
gatt-out/

# machine-local MCP registration (absolute paths)
.mcp.json
swagger.json
gatt.exe

# release artifacts
dist/

# local scratch / private dumps
test_ts.go
gen_graph.py
whatsappCRM_schema.sql
